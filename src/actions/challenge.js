import { types } from "../types/types";

export const test0=()=>({
  type:types.punto0,
  payload:{ test:"Punto 0"}

});

export const test1=()=>({
  type:types.punto1,
  payload:{ test:"Punto 1"}

});

export const test2=()=>({
  type:types.punto2,
  payload:{ test:"Punto 2"}

});
export const test3=()=>({
  type:types.punto3,
  payload:{ test:"Punto 3"}

});

export const test4=()=>({
  type:types.punto4,
  payload:{ test:"Punto 4"}

});
export const test5=()=>({
  type:types.punto5,
  payload:{ test:"Punto 5"}

});
